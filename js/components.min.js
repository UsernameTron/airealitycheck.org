function getPathToRoot() {// Get the pathname from the current URLlet path = window.location.pathname;// Handle development environments with missing trailing slashesif (!path.endsWith('/') && !path.endsWith('.html')) {path += '/';}// Split path into segments and filter out empty segmentsconst segments = path.split('/').filter(segment => segment.length > 0);// Special handling for root or direct HTML files under rootif (segments.length === 0) {return ''; // At root}if (segments.length === 1 && segments[0].endsWith('.html')) {return ''; // HTML file at root level}// Count how many directories deep we are for proper path calculationlet depth = segments.length;// If the last segment is an HTML file, reduce the depth by 1if (segments.length > 0 && segments[segments.length - 1].endsWith('.html')) {depth--;}// Generate the appropriate number of "../" based on depthlet rootPath = '';for (let i = 0; i < depth; i++) {rootPath += '../';}return rootPath;}