# External Integrations

> Auto-generated by gsd-codebase-mapper | Focus: tech | 2026-04-03

## Summary

Minimal external integration footprint. The site relies on Google Fonts for typography, YouTube for embedded video content, jsDelivr CDN for one charting library, and GitHub Pages for hosting/deployment. No analytics, no authentication, no databases.

## APIs & External Services

**Google Fonts:**
- Used for all typography across the site
- Preconnect to `fonts.googleapis.com` and `fonts.gstatic.com`
- Main site loads: Geist, Geist Mono
- Career content loads: Product Sans, Roboto
- Referenced in: `index.html` (line 30-32), all `career-content/` HTML files

**YouTube (Embedded Video):**
- Lazy-loaded iframes via thumbnail click pattern in `js/site.js` (lines 49-75)
- Thumbnail images fetched from `https://img.youtube.com/vi/{id}/maxresdefault.jpg`
- Iframe embeds from `https://www.youtube.com/embed/{id}?autoplay=1&rel=0`
- Video IDs stored in `content/data.json` and hardcoded in `index.html`
- 5+ videos embedded (satire, creative content)

**jsDelivr CDN:**
- Chart.js 4.4.0 loaded in `career-content/resources/roi-calculator.html` (line 349)
- URL: `https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js`
- Single-page dependency for ROI calculator charts

## Data Storage

**Databases:**
- None. Fully static site.

**File Storage:**
- All assets served from the repository itself via GitHub Pages
- Images stored in `images/` and `career-content/*/images/`
- No external image hosting or CDN for site assets

**Caching:**
- Browser caching only (GitHub Pages default headers)
- Custom `_headers` file sets CSP but no explicit cache headers

**Local Storage (Browser):**
- Theme preference persisted via `localStorage`:
  - Key `theme` in `js/site.js` (line 464)
  - Key `arc-theme` in `career-content/js/loader.min.js` (line 5) and `career-content/js/main.min.js` (line 15)
- Note: Two different localStorage keys for the two design systems

## Authentication & Identity

**Auth Provider:**
- None. No login, no user accounts, no authentication.

## Monitoring & Observability

**Error Tracking:**
- None.

**Analytics:**
- None detected. No Google Analytics, no gtag, no Plausible, no Fathom.
- CSP headers whitelist `https://www.googletagmanager.com` (suggesting analytics was planned or previously used), but no analytics scripts are loaded.

**Logs:**
- None. Static site with no server-side logging.

## CI/CD & Deployment

**Hosting:**
- GitHub Pages
- Custom domain: `airealitycheck.org` (configured via `CNAME`)
- HTTPS provided by GitHub Pages

**CI Pipeline:**
- GitHub Actions: `.github/workflows/deploy.yml`
- Trigger: Push to `main` branch or manual `workflow_dispatch`
- Steps: Checkout -> Configure Pages -> Upload artifact (entire repo root) -> Deploy
- Uses: `actions/checkout@v5`, `actions/configure-pages@v5`, `actions/upload-pages-artifact@v4`, `actions/deploy-pages@v5`
- Concurrency group `"pages"` prevents parallel deploys
- Deploy time: ~2 minutes after push to main

**No build step in pipeline.** The entire repo root is uploaded as-is.

## Environment Configuration

**Required env vars:**
- None. No environment variables used anywhere.

**Secrets:**
- No secrets required. GitHub Pages deployment uses built-in `GITHUB_TOKEN` via `id-token: write` permission.

## Content-Security-Policy

Defined in two places:
1. `_headers` file (root) — Applied by GitHub Pages at the hosting level
2. `<meta http-equiv="Content-Security-Policy">` — Inline in many career-content HTML files

**Policy allows:**
- `default-src 'self'`
- `script-src 'self' 'unsafe-inline' https://www.youtube.com https://www.googletagmanager.com`
- `style-src 'self' 'unsafe-inline' https://fonts.googleapis.com`
- `font-src 'self' https://fonts.gstatic.com`
- `img-src 'self' data: https:`
- `frame-src https://www.youtube.com`
- `connect-src 'self'`
- `object-src 'none'`

**Note:** The CSP does not whitelist `cdn.jsdelivr.net`, which means the Chart.js load in `career-content/resources/roi-calculator.html` may be blocked by CSP on pages that set the meta header. That page does not set the meta CSP header, so it works via the `_headers` file CSP (which also does not whitelist jsDelivr — potential issue).

## Social Links & External References

**Outbound links in `index.html` and `content/data.json`:**
- LinkedIn: `https://linkedin.com/in/cpconnor`
- GitHub: `https://github.com/UsernameTron`
- YouTube: `https://youtube.com/@CPeteConnor`
- Substack: `https://substack.com/@cpconnor`
- X/Twitter: `https://x.com/connor_pet68383`

**GitHub repository links** in `career-content/portfolio/cxer-ml-ai-triage-kit.html` reference 10+ public repos under `github.com/UsernameTron/`.

## Webhooks & Callbacks

**Incoming:**
- None.

**Outgoing:**
- None.

## Observations

1. **No analytics integration** — The site has no way to track visitor behavior. The CSP whitelists Google Tag Manager but no tracking scripts are loaded.
2. **CSP vs jsDelivr conflict** — Chart.js loaded from jsDelivr CDN is not whitelisted in the Content-Security-Policy. This could silently break the ROI calculator page.
3. **Dual localStorage keys** — The main site uses `theme` while career-content uses `arc-theme` for theme persistence. Navigating between the two design systems does not share theme preference.
4. **No external API calls** — The only `fetch()` call is to the local `content/data.json` file. The site is entirely self-contained.

---

*Integration audit: 2026-04-03*
