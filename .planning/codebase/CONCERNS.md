# Codebase Concerns

> Auto-generated by gsd-codebase-mapper | Focus: concerns | 2026-04-03

## Summary

The static site at airealitycheck.org has systemic SEO issues (wrong canonical URLs across all career-content pages), placeholder images never replaced with real content, stub JS files loaded for no reason, and an incomplete design system migration. Accessibility coverage is inconsistent -- about half the pages have zero ARIA attributes. No console.log violations or empty alt text detected.

## Critical Issues

**Canonical URLs omit `career-content/` path segment:**
- Issue: Every canonical URL and og:url in `career-content/` pages points to a path without the `career-content/` prefix. The actual deployed URLs include this prefix since GitHub Pages serves from repo root.
- Files: All 13 pages with canonical tags, including:
  - `career-content/articles/detection.html` — canonical says `/articles/detection.html`, actual URL is `/career-content/articles/detection.html`
  - `career-content/portfolio/tools.html` — canonical says `/portfolio/tools.html`
  - `career-content/case-studies/index.html` — canonical says `/case-studies/`
  - `career-content/resources/index.html` — canonical says `/resources/`
- Impact: Search engines receive incorrect canonical signals. Could cause indexing failures, duplicate content penalties, or pages not appearing in search results at all.
- Fix approach: Add `career-content/` to every canonical and og:url href. Batch find-and-replace across all 13+ affected files.

**Broken canonical URL in counterfactual reasoning article:**
- Issue: The canonical URL is malformed — two filenames concatenated without a separator.
- File: `career-content/articles/counterfactual-reasoning-html.html` line 9
- Current value: `https://airealitycheck.org/articles/counterfactual-reasoning-html.htmlcx-counterfactual-reasoning.html`
- Impact: Completely invalid URL. Search engines cannot resolve this.
- Fix approach: Correct to `https://airealitycheck.org/career-content/articles/counterfactual-reasoning-html.html`

**11 pages missing canonical URLs entirely:**
- Issue: No `<link rel="canonical">` tag present.
- Files:
  - `career-content/case-studies/hr-predictive-model.html`
  - `career-content/case-studies/ml-bpo-turnover-wfm.html`
  - `career-content/case-studies/contact-center-analytics-AI-Executive-Overview.html`
  - `career-content/case-studies/linkedin-visibility-google-style.html`
  - `career-content/articles/automation-strategy-article.html`
  - `career-content/resources/prompt-workspace.html`
  - `career-content/resources/ai-readiness-assessment.html`
  - `career-content/resources/roi-calculator.html`
  - `career-content/portfolio/cxer-ml-ai-triage-kit.html`
  - `career-content/portfolio/tiktok-dashboard-google-style-1.html`
  - `career-content/portfolio/social-media-analytics.html`
- Impact: No canonical signal for these pages. Search engines guess on their own.
- Fix approach: Add canonical tags with correct `career-content/`-prefixed URLs to each file's `<head>`.

**Homepage (`index.html`) missing canonical URL:**
- File: `index.html`
- Impact: Homepage has no canonical tag. Less critical since it is the root, but still a best-practice gap.
- Fix approach: Add `<link rel="canonical" href="https://airealitycheck.org/">` to `<head>`.

## Moderate Issues

**Placeholder images never replaced with real content:**
- Issue: 7 `<img>` tags across 5 pages reference `images/placeholder.svg` — a 250-byte generic SVG. These were meant to be temporary.
- Files:
  - `career-content/portfolio/tiktok-dashboard-google-style-1.html` (1 placeholder)
  - `career-content/portfolio/social-media-analytics.html` (1 placeholder)
  - `career-content/case-studies/ml-bpo-turnover-wfm.html` (2 placeholders)
  - `career-content/articles/index.html` (1 placeholder — hero image)
  - `career-content/articles/cx-and-the-fine-tuned-open-source-llm.html` (2 placeholders)
- Impact: Unprofessional appearance on portfolio/case study pages intended for executive audiences. The articles index hero is particularly visible.
- Fix approach: Create or source real images for each context. Prioritize the articles index hero and case study diagrams.

**Stub JavaScript files loaded but empty:**
- Issue: Two JS files contain only a comment — no actual code — but are loaded via `<script>` tags in 5 HTML pages.
- Files:
  - `career-content/js/scroll-animations.js` (2 lines, comment only)
  - `career-content/js/video-player.js` (2 lines, comment only)
- Loaded by: `career-content/articles/index.html`, `career-content/portfolio/index.html`, `career-content/portfolio/oppy-video.html`, `career-content/portfolio/bpo-wfm-video.html`, `career-content/case-studies/index.html`
- Impact: Two unnecessary HTTP requests per page load. Minor performance hit but signals incomplete implementation.
- Fix approach: Either implement the functionality or remove the `<script>` tags from the 5 HTML files and delete the stub files.

**Obsidian bridge CSS loaded by only 4 of 25 career-content pages:**
- Issue: `career-content/css/obsidian-bridge.css` (the transitional stylesheet mapping Google design tokens to Obsidian) is only loaded by the 4 index pages. The remaining 21 career-content pages still use the original Google-inspired design system without the bridge.
- Files loading bridge: `career-content/articles/index.html`, `career-content/case-studies/index.html`, `career-content/portfolio/index.html`, `career-content/resources/index.html`
- Files NOT loading bridge: All individual article, case study, portfolio, and resource pages (21 files)
- Impact: Visual inconsistency — index pages look Obsidian-themed while detail pages look Google-themed. Users see a jarring design switch when navigating from index to detail.
- Fix approach: Either add the obsidian-bridge.css import to all 21 remaining pages, or accept the dual-design-system as intentional and remove the bridge from index pages for consistency.

**Duplicate og:url meta tag in profile page:**
- File: `career-content/portfolio/profile-google-style.html` lines 56 and 106
- Impact: Two `og:url` tags in the same page. Social media scrapers may behave unpredictably.
- Fix approach: Remove the duplicate on line 106.

**Accessibility gaps — 9 pages have zero ARIA attributes or role attributes:**
- Files with no ARIA/role markup:
  - `career-content/case-studies/hr-predictive-model.html`
  - `career-content/case-studies/ml-bpo-turnover-wfm.html`
  - `career-content/case-studies/contact-center-analytics-AI-Executive-Overview.html`
  - `career-content/case-studies/linkedin-visibility-google-style.html`
  - `career-content/articles/automation-strategy-article.html`
  - `career-content/resources/prompt-workspace.html`
  - `career-content/resources/ai-readiness-assessment.html`
  - `career-content/portfolio/cxer-ml-ai-triage-kit.html`
  - `career-content/portfolio/social-media-analytics.html`
- Impact: Screen readers get no semantic navigation hints on these pages. Given the portfolio is aimed at enterprise audiences, accessibility matters.
- Fix approach: Add `role="main"`, `role="navigation"`, `aria-label` on interactive elements, and landmark regions to each page. Follow the pattern already used in `index.html` (15 ARIA attributes).

## Minor Issues

**Orphaned file at repo root:**
- File: `24A36E46-A71D-4E4D-9320-D9DF8AF54A1D.jpeg` (180KB)
- Issue: UUID-named JPEG sitting in repo root. Not referenced by any HTML file. Appears to be an accidental commit from a mobile device or screenshot tool.
- Impact: Deployed to production unnecessarily. Minor clutter.
- Fix approach: Verify it is unused, then delete.

**Development artifacts committed to repo:**
- Files: `designplan.md` (864 lines), `claudeprompt.md` (250 lines)
- Issue: Internal planning/prompt documents deployed to production alongside the site. Not linked from any page but publicly accessible.
- Impact: Exposes internal development process to anyone who guesses the URL. No security risk but unprofessional.
- Fix approach: Move to `.planning/` or add to `.gitignore` and remove from the deployed site.

**Large hero image not optimized:**
- File: `career-content/articles/images/articleshero.webp` (1.1MB)
- Impact: Slow load for articles index page hero. WebP format is good but the file is oversized for web use.
- Fix approach: Resize to max 1920px width and compress to under 200KB.

**`_headers` file has no effect on GitHub Pages:**
- File: `_headers` (Content-Security-Policy rules)
- Issue: The `_headers` file format is for Netlify/Cloudflare Pages, not GitHub Pages. GitHub Pages does not read this file.
- Impact: The CSP rules defined here are not applied. The site runs without Content-Security-Policy headers.
- Fix approach: If CSP is desired, implement via `<meta http-equiv="Content-Security-Policy">` in each HTML `<head>`, or migrate hosting to a platform that supports `_headers`.

**Sitemap lastmod dates all frozen at 2026-02-19:**
- File: `sitemap.xml`
- Issue: Every URL has the same `lastmod` date. The sitemap does not reflect actual modification dates.
- Impact: Search engines may deprioritize the sitemap as unreliable, reducing crawl efficiency.
- Fix approach: Update lastmod dates when pages are modified, or remove the `lastmod` field entirely if manual maintenance is impractical.

**Stray `content/` directory with minimal content:**
- Files: `content/data.json`, `content/publications/Crushin-Claude.pdf`
- Issue: This directory sits outside the main site structure. `data.json` and the PDF are not obviously referenced from any page.
- Impact: Clutter. The PDF is publicly accessible at a guessable URL.
- Fix approach: Verify references, relocate to appropriate directory, or remove.

**CSS naming: `style.min.css` is not actually minified:**
- File: `career-content/css/style.min.css` (717 lines)
- Issue: The `.min.css` suffix implies minified CSS, but the file is fully formatted with whitespace and comments. Additionally, `style.css` is a 4-line file that just `@import`s `style.min.css`.
- Impact: Misleading file naming. No real performance issue since it is a small file.
- Fix approach: Either minify the file to match its name, or rename to `style.css` and remove the import alias.

## Observations

**Overall health: Functional but inconsistent.** The site works and serves content correctly. The two design systems (Obsidian for homepage, Google-inspired for career content) are intentional per CLAUDE.md, but the half-applied obsidian-bridge.css creates a third visual state that undermines both.

**SEO is the highest-priority fix area.** Every canonical URL is wrong. This is a systematic issue that affects all 25 pages and directly impacts search visibility for a site that serves as a professional portfolio.

**No code quality violations detected.** Zero console.log statements in JS, all images have alt text, all pages have viewport meta tags, all pages declare `<html lang="en">`. The codebase is clean of the most common static site sins.

**The `content/data.json` and publications directory suggest planned features that were never wired up.** Combined with the stub JS files and placeholder images, there is a pattern of scaffolded-but-unfinished features.
