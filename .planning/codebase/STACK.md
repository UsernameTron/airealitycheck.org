# Technology Stack

> Auto-generated by gsd-codebase-mapper | Focus: tech | 2026-04-03

## Summary

Pure static site with zero build toolchain. HTML, CSS, and vanilla JavaScript served directly from the repository root via GitHub Pages. No package manager, no bundler, no framework — files are authored and deployed as-is.

## Languages

**Primary:**
- HTML5 — All page content (~25 HTML files across root and `career-content/`)
- CSS3 — Two design systems, custom properties throughout
- JavaScript (ES6+) — Vanilla JS, no frameworks or transpilation

**Secondary:**
- JSON — Data-driven content (`content/data.json`)
- XML — Sitemap (`sitemap.xml`)
- YAML — GitHub Actions workflow (`.github/workflows/deploy.yml`)
- SVG — Favicon (`favicon.svg`), inline icons in HTML

## Runtime

**Environment:**
- Browser-only. No server-side runtime.
- No Node.js, Python, or any backend.

**Package Manager:**
- None. No `package.json`, `requirements.txt`, or lockfile exists.

**Local Development:**
```bash
python -m http.server 8000   # Serve from repo root
```

## Frameworks

**Core:**
- None. Pure hand-written HTML/CSS/JS.

**Testing:**
- None detected. No test framework, no test files.

**Build/Dev:**
- None. No build step, no minifier, no preprocessor.
- GitHub Actions deploys raw files: `.github/workflows/deploy.yml`

## Key Dependencies

**CDN-Loaded (runtime):**
- Chart.js 4.4.0 — Used only in `career-content/resources/roi-calculator.html` via jsDelivr CDN
  - `https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js`

**Google Fonts (runtime):**
- Geist (sans) + Geist Mono — Main site (`index.html`)
- Product Sans + Roboto — Career content pages
- Loaded via `https://fonts.googleapis.com` with `preconnect` hints

**No other external JavaScript libraries.** All interactivity is vanilla JS.

## CSS Architecture

**Two independent design systems:**

| System | Stylesheet | Used By | Lines |
|--------|-----------|---------|-------|
| Obsidian | `css/obsidian.css` | `index.html` (homepage) | 1,674 |
| Google-Inspired | `career-content/css/style.min.css` | All `career-content/` pages | 717 |

**Supporting CSS files:**
- `career-content/css/theme-variables.css` — CSS custom properties only
- `career-content/css/article-overrides.css` — Article-specific overrides
- `career-content/css/obsidian-bridge.css` — Bridge between the two design systems (219 lines)
- `career-content/css/style.css` — Alias that imports `style.min.css`

**Note:** Despite the `.min.css` name, `style.min.css` is human-readable source — not minified output.

## JavaScript Architecture

**Main site:**
- `js/site.js` (608 lines) — IIFE pattern, strict mode. Handles carousel, lazy YouTube loading, data-driven POCs/articles via `fetch('content/data.json')`, gallery filters, lightbox, smooth scroll, theme toggle.

**Career content:**
- `career-content/js/loader.min.js` (12 lines) — Theme persistence loader, runs before render
- `career-content/js/main.min.js` (32 lines) — Theme toggle + mobile nav
- `career-content/js/scroll-animations.js` (2 lines) — Placeholder/stub
- `career-content/js/video-player.js` (2 lines) — Placeholder/stub

**Resource pages** (`career-content/resources/`) are fully self-contained with embedded `<style>` and `<script>` blocks.

## Configuration

**Environment:**
- No environment variables. No `.env` files.
- All configuration is inline in HTML/CSS/JS.

**Build:**
- No build configuration files exist.

**SEO/Metadata:**
- `robots.txt` — Allows all crawlers, references sitemap
- `sitemap.xml` — Manual XML sitemap covering all pages
- `CNAME` — Custom domain: `airealitycheck.org`
- OpenGraph + Twitter Card meta tags on `index.html`

**Security Headers:**
- `_headers` file — Content-Security-Policy for GitHub Pages
- CSP also set via `<meta http-equiv="Content-Security-Policy">` on many career-content pages

## Platform Requirements

**Development:**
- Any HTTP server (Python, VS Code Live Server, etc.)
- No build tools required
- Any modern browser for testing

**Production:**
- GitHub Pages (static hosting)
- Custom domain via CNAME DNS record
- HTTPS provided by GitHub Pages

---

*Stack analysis: 2026-04-03*
