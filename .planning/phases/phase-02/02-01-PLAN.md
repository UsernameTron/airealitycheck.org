---
phase: 02-consolidate-verification-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/gsd/crew.md
  - ~/.claude/CLAUDE.md
  - ~/.claude/commands/gsd/plan-phase.md
  - ~/.claude/get-shit-done/workflows/quick.md
  - ~/.claude/get-shit-done/references/model-profiles.md
autonomous: true
requirements: [PHASE-02]

must_haves:
  truths:
    - "No file in ~/.claude/ references gsd-plan-checker as a standalone agent"
    - "No file in ~/.claude/ references gsd-integration-checker as a standalone agent"
    - "No file in ~/.claude/ references gsd-nyquist-auditor as a standalone agent"
    - "All former references now use gsd-verifier (scope: X) pattern per D5"
    - "CHANGELOG.md and gsd-tools.cjs are untouched per D2 and D3"
  artifacts:
    - path: "~/.claude/commands/gsd/crew.md"
      provides: "Updated roster, spawning map, and relationship descriptions"
    - path: "~/.claude/CLAUDE.md"
      provides: "Updated quality gates section"
    - path: "~/.claude/commands/gsd/plan-phase.md"
      provides: "Updated objective text"
    - path: "~/.claude/get-shit-done/references/model-profiles.md"
      provides: "Consolidated model profile rows"
  key_links:
    - from: "crew.md roster"
      to: "~/.claude/agents/gsd-verifier.md"
      via: "Agent names in roster match actual agent files"
      pattern: "gsd-verifier.*scope"
---

<objective>
Update all 5 documentation/reference files that still cite gsd-plan-checker, gsd-integration-checker, and gsd-nyquist-auditor as standalone agents. Replace with gsd-verifier (scope: X) pattern per D5.

Purpose: Complete Phase 2 by ensuring all documentation reflects the already-completed agent consolidation.
Output: 5 updated files with consistent references to the unified gsd-verifier agent.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-02/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update crew.md roster, spawning map, and relationship examples</name>
  <files>~/.claude/commands/gsd/crew.md</files>
  <action>
This file has the most references to update. Make these specific changes:

**PLANNING section (line 76):**
- Remove: `gsd-plan-checker ......... Verifies plans achieve phase goal before execution`
- Do NOT add a replacement here — plan verification is now a scope of gsd-verifier which is already listed in VALIDATION section

**VALIDATION section (lines 90-92):**
- Keep: `gsd-verifier ............. Verifies phase goal achievement (goal-backward)`
- Remove: `gsd-integration-checker .. Checks cross-phase integration`
- Remove: `gsd-nyquist-auditor ...... Fills validation gaps in completed phases`
- Update gsd-verifier description to: `gsd-verifier ............. Unified verification (scopes: general, plan, integration, nyquist)`

**SPAWNING MAP (lines 107-109, 116):**
- Line 107: Change `phase-researcher → planner → plan-checker` to `phase-researcher → planner → verifier (scope: plan)`
- Line 108: Change `executor (+ debugger, verifier, nyquist-auditor on failure)` to `executor (+ debugger, verifier on failure)`
- Line 109: Change `verifier, integration-checker` to `verifier (scope: general), verifier (scope: integration)`
- Line 116: Change `nyquist-auditor` to `verifier (scope: nyquist)`

**Works with section (lines 139-141):**
- Line 140: Change `→ gsd-plan-checker (validates output)` to `→ gsd-verifier (scope: plan) (validates output)`

**Recommend example (lines 243):**
- Line 243: Change `gsd-plan-checker — will verify plan covers security requirements` to `gsd-verifier (scope: plan) — will verify plan covers security requirements`

Per D5: Use `gsd-verifier (scope: X)` pattern to preserve routing context.
  </action>
  <verify>
    <automated>grep -n "plan-checker\|integration-checker\|nyquist-auditor" ~/.claude/commands/gsd/crew.md | grep -v "CHANGELOG\|History\|Absorbed" ; echo "Exit: $?"</automated>
  </verify>
  <done>Zero grep matches for old standalone agent names in crew.md (excluding any historical/attribution comments). All references use gsd-verifier (scope: X) pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md, plan-phase.md, quick.md, and model-profiles.md</name>
  <files>~/.claude/CLAUDE.md, ~/.claude/commands/gsd/plan-phase.md, ~/.claude/get-shit-done/workflows/quick.md, ~/.claude/get-shit-done/references/model-profiles.md</files>
  <action>
**~/.claude/CLAUDE.md (lines 149-152) — Quality Gates section:**
Replace the 3-bullet quality agents list:
```
    - **gsd-plan-checker** — Validates plan completeness before execution
    - **gsd-verifier** — Validates implementation against spec
    - **gsd-integration-checker** — Cross-component integration validation
```
With a single consolidated entry:
```
    - **gsd-verifier** — Unified verification agent (scopes: general for implementation, plan for completeness, integration for cross-component, nyquist for gap detection)
```
Per D5: the scope descriptions preserve what each old agent did.

**~/.claude/commands/gsd/plan-phase.md (line 21):**
Change: `verify with gsd-plan-checker`
To: `verify with gsd-verifier (scope: plan)`

**~/.claude/get-shit-done/workflows/quick.md:**
Line 22-23 area already references the unified agent correctly. Confirm no stale references remain. If `plan-checker` appears anywhere in the file, replace with `gsd-verifier (scope: plan)`.

**~/.claude/get-shit-done/references/model-profiles.md (lines 18-20):**
Remove these 3 rows:
```
| gsd-plan-checker | sonnet | sonnet | haiku | inherit |
| gsd-integration-checker | sonnet | sonnet | haiku | inherit |
| gsd-nyquist-auditor | sonnet | sonnet | haiku | inherit |
```
The gsd-verifier row (line 17) already exists and covers all scopes.

Per D2: Do NOT touch CHANGELOG.md.
Per D3: Do NOT touch gsd-tools.cjs.
  </action>
  <verify>
    <automated>echo "=== CLAUDE.md ===" && grep -n "plan-checker\|integration-checker\|nyquist-auditor" ~/.claude/CLAUDE.md; echo "=== plan-phase.md ===" && grep -n "plan-checker\|integration-checker\|nyquist-auditor" ~/.claude/commands/gsd/plan-phase.md; echo "=== quick.md ===" && grep -n "plan-checker\|integration-checker\|nyquist-auditor" ~/.claude/get-shit-done/workflows/quick.md; echo "=== model-profiles.md ===" && grep -n "plan-checker\|integration-checker\|nyquist-auditor" ~/.claude/get-shit-done/references/model-profiles.md; echo "Exit: $?"</automated>
  </verify>
  <done>Zero grep matches for old standalone agent names across all 4 files. Quality gates section shows single gsd-verifier entry. Model profiles table has no duplicate rows for absorbed agents.</done>
</task>

</tasks>

<verification>
Final sweep: confirm no stale references remain in any of the 5 modified files, AND confirm excluded files are untouched:

```bash
# Stale reference check across all 5 files
grep -rn "gsd-plan-checker\|gsd-integration-checker\|gsd-nyquist-auditor" \
  ~/.claude/commands/gsd/crew.md \
  ~/.claude/CLAUDE.md \
  ~/.claude/commands/gsd/plan-phase.md \
  ~/.claude/get-shit-done/workflows/quick.md \
  ~/.claude/get-shit-done/references/model-profiles.md

# Confirm CHANGELOG untouched (per D2)
git diff ~/.claude/get-shit-done/CHANGELOG.md 2>/dev/null || echo "No CHANGELOG changes (correct)"

# Confirm gsd-tools.cjs untouched (per D3)
git diff ~/.claude/get-shit-done/bin/gsd-tools.cjs 2>/dev/null || echo "No gsd-tools changes (correct)"
```

Expected: zero matches in stale reference check, no diff on excluded files.
</verification>

<success_criteria>
- All 5 files updated with gsd-verifier (scope: X) pattern
- Zero grep matches for gsd-plan-checker, gsd-integration-checker, or gsd-nyquist-auditor as standalone agents in modified files
- CHANGELOG.md untouched (per D2)
- gsd-tools.cjs untouched (per D3)
- All references preserve the scope/routing context (per D5)
</success_criteria>

<output>
After completion, create `.planning/phases/phase-02/02-01-SUMMARY.md`
</output>
